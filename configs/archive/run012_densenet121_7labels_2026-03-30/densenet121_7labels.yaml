experiment_name: "densenet121_7labels"

# ── Data ──
data:
  data_dir: "CheXpert-v1.0-small"
  train_csv: "CheXpert-v1.0-small/train_proper.csv"
  valid_csv: "CheXpert-v1.0-small/val_proper.csv"
  test_csv:  "CheXpert-v1.0-small/test.csv"
  image_size: 224
  frontal_only: true

  # Per-label strategy for primary/incidental findings study
  uncertainty_strategy: "u-mixed"
  per_label_strategies:
    Cardiomegaly: "u-zeros"
    Edema: "u-ones"
    Consolidation: "u-ones"
    Atelectasis: "u-ones"
    Pleural Effusion: "u-zeros"
    Fracture: "u-ones"
    Pneumothorax: "u-ones"

# ── Labels ──
labels:
  target_labels:
    - "Cardiomegaly"
    - "Edema"
    - "Consolidation"
    - "Atelectasis"
    - "Pleural Effusion"
    - "Fracture"
    - "Pneumothorax"

# ── Model ──
model:
  architecture: "densenet121"
  pretrained: true
  num_classes: 7

# ── Training ──
training:
  batch_size: 64
  num_workers: 4
  epochs: 20
  learning_rate: 0.0001    # CNNs tolerate higher LR than transformers
  weight_decay: 0.01
  warmup_epochs: 1
  scheduler: "cosine"
  early_stopping_patience: 5
  pos_weight: true

# ── Device ──
device: "auto"

# ── Logging ──
logging:
  checkpoint_dir: "checkpoints"
  log_dir: "runs"
  save_best_only: true
